services:
  - type: web
    name: ai-legal-system
    env: python
    runtime: python
    buildCommand: pip install -r backend/requirements.txt && python backend/ml_model.py
    startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT